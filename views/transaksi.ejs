
<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.13.1/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/style.css">
    <style>
        body {
            font-family: 'Poppins', Arial, sans-serif;
            background: #f8f8fa;
            margin: 0;
        }
        .transaksi-container {
            max-width: 700px;
            margin: 120px auto 40px auto;
            background: #fff;
            border-radius: 18px;
            box-shadow: 0 4px 24px rgba(0,0,0,0.08);
            padding: 32px 28px 24px 28px;
        }
        .transaksi-header {
            border-bottom: 1px solid #ececec;
            margin-bottom: 24px;
            padding-bottom: 18px;
        }
        .transaksi-header h2 {
            font-size: 2rem;
            font-weight: 700;
            color: #c72825;
            margin: 0 0 8px 0;
        }
        .transaksi-header .meta {
            font-size: 1rem;
            color: #888;
            margin-bottom: 4px;
        }
        .transaksi-header .total {
            font-size: 1.2rem;
            font-weight: 600;
            color: #e17055;
        }
        .produk-list {
            margin-top: 18px;
        }
        .produk-card {
            background: #f9f9fc;
            border-radius: 12px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.04);
            padding: 18px 20px;
            margin-bottom: 18px;
            display: flex;
            flex-direction: column;
        }
        .produk-card h4 {
            font-size: 1.1rem;
            font-weight: 600;
            margin: 0 0 6px 0;
            color: #c72825;
        }
        .produk-card p {
            margin: 2px 0;
            font-size: 0.98rem;
            color: #444;
        }
        .produk-card .produk-status {
            font-size: 0.95rem;
            color: #e17055;
            font-weight: 500;
            margin-top: 6px;
        }
        @media (max-width: 800px) {
            .transaksi-container {
                max-width: 98vw;
                padding: 18px 8px 12px 8px;
            }
        }
    </style>
</head>
<body>
    <div class="">
        <div class="parent">
            <nav class="navbar">
                <div class="navbar-left">
                    <a href="/" class="logo-link"><img class="logo" src="/logo.png" alt="Rice Katsu Logo"></a>
                </div>
                <div class="navbar-right">
                    <% if (user) { %>
                        <span class="user-greeting">Halo, <%= user.name || user.email %></span>
                        <a href="/logout" class="icon" title="Logout"><i class="bi bi-box-arrow-in-left"></i></a>
                    <% } else { %>
                        <a href="/login" class="icon" title="Login"><i class="bi bi-person-circle"></i></a>
                    <% } %>
                </div>
            </nav>


            <div class="transaksi-container">
                <div class="transaksi-header">
                    <h2>Transaksi #<%= order.id_order %></h2>
                    <div class="meta">Status: <strong><%= order.status %></strong></div>
                    <div class="meta">Tanggal: <%= new Date(order.tanggal).toLocaleString('id-ID') %></div>
                    <div class="total">Total: Rp <%= order.total.toLocaleString('id-ID') %></div>
                </div>
                <div class="produk-list">
                    <h3 style="margin-bottom:16px;font-size:1.1rem;color:#c72825;font-weight:600;">Produk yang dipesan:</h3>
                    <% if (items && items.length > 0) { %>
                        <% items.forEach(item => { %>
                            <div class="produk-card">
                                <h4><%= item.nama_produk %></h4>
                                <p>Harga: Rp <%= item.harga_produk.toLocaleString('id-ID') %></p>
                                <p>Jumlah: <%= item.jumlah_produk %></p>
                                <p>Total: <strong>Rp <%= item.total.toLocaleString('id-ID') %></strong></p>
                            </div>
                        <% }) %>
                    <% } else { %>
                        <p style="color:#888;">Tidak ada produk pada transaksi ini.</p>
                    <% } %>
                </div>
            </div>

            <footer class="footer">
                <p>&copy; 2025 Rice Katsu - Semua Hak Dilindungi</p>
            </footer>
        </div>
    </div>
</body>
</html>
