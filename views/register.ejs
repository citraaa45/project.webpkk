<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
        /* Gaya dasar dipertahankan dari login.ejs */
        body {
            font-family: 'Poppins', Arial, sans-serif;
            background: linear-gradient(135deg, #fff6d1, #ffd87a, #f4c55c);
            height: 100vh;
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .card {
            width: 360px;
            background: rgba(255, 255, 255, 0.9);
            padding: 25px;
            border-radius: 20px;
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
        }

        h2 { text-align: center; color: #3a3a3a; margin-top: 10px; margin-bottom: 25px; }
        label { display: block; margin-bottom: 5px; font-weight: 600; color: #555; }

        input {
            width: 93%; padding: 10px; margin-bottom: 20px; border-radius: 10px;
            border: 1px solid #d8d8d8; outline: none; font-size: 14px;
        }
        input:focus { border-color: #ffbb38; box-shadow: 0 0 5px rgba(255, 187, 56, 0.5); }
        .btn {
            padding: 12px; background: linear-gradient(135deg, #4CAF50, #45a049); /* Warna hijau untuk register */
            border-radius: 12px; border: none; color: white;
            font-size: 16px; font-weight: bold; cursor: pointer; width: 100%;
            transition: 0.3s;
        }
        
        .error-message { color: rgb(220, 53, 69); text-align: center; margin-top: 15px; font-weight: 500; }
        .login-link { text-align: center; margin-top: 15px; font-size: 14px; }
        .login-link a { color: #555; text-decoration: none; font-weight: bold; }
    </style>
</head>

<body>

<form class="card" action="/register" method="POST">
    <h2>DAFTAR AKUN BARU</h2>

    <p class="error-message">
        <% if (typeof error !== 'undefined' && error) { %>
            <%= error %>
        <% } %>
    </p>

    <label for="username">USERNAME</label>
    <input type="text" id="username" name="username" required>

    <label for="nama">NAMA LENGKAP</label>
    <input type="text" id="nama" name="nama" required>

    <label for="password">PASSWORD</label>
    <input type="password" id="password" name="password" required>
    
    <input type="hidden" name="role" value="pembeli">

    <button class="btn" type="submit">DAFTAR</button>
    
    <div class="login-link">
        Sudah punya akun? <a href="/login">Login di sini</a>
    </div>
</form>

</body>
</html>